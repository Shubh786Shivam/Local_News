<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginActivity">
        <activity
            android:name=".MainActivity2"
            android:label="@string/title_activity_main2"></activity>
        <activity
            android:name=".NewsDetailsActivity"
            android:label="Details"></activity>
        <activity android:name=".ListArticleSummary" />
        <activity
            android:name=".AuthoriseArticleUserAdmin"
            android:label="Admin Login" />
        <activity
            android:name=".AddNewsUserLoggedIn"
            android:label="User Login" />
        <activity
            android:name=".HomePage"
            android:label="News" />
        <activity
            android:name=".AuthoriseActivity"
            android:label="Authorise" />
        <activity
            android:name=".AddNewsActivity"
            android:label="Add News" />
        <activity
            android:name=".LoginActivity"
            android:label="Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>